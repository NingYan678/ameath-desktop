# 爱弥斯（Ameath）Hermes 桌面终端

爱弥斯是一个运行在 Windows 上的 PySide6 桌面宠物，也是 Hermes 的原生桌面终端。它以透明悬浮窗口、角色动画和连续对话面板提供轻量的桌面交互；人格、记忆、技能、工具、任务与操作确认则统一由同一个 Hermes Gateway 管理。

## 项目定位

项目不是独立的聊天机器人或另一套 AI 后端。桌宠仅负责显示与输入，Hermes 负责智能能力与执行边界：

- 桌宠消息进入稳定的 Hermes 桌面会话。
- 已安装的 Hermes 技能、长期记忆、工具和任务可由桌宠使用。
- 流式输出、工具运行状态、澄清问题和原生授权请求都会显示在桌宠界面中。
- 桌宠不会自行绕过 Hermes 的权限策略，也不会另建一套提醒或任务系统。

## 已完成能力

### 桌宠与对话体验

- Windows + Python + PySide6 的透明、可置顶桌面悬浮窗口。
- 动画状态框架，可对应等待、处理中、回复完成、通知与确认等交互状态。
- 自适应连续对话面板：
  - 紧凑状态仅显示最近一对用户/爱弥斯对话；
  - 用户交互、流式回复、通知或确认请求到达时自动展开；
  - 展开状态可显示连续消息、滚动阅读长回复；
  - 空闲后自动收缩，输入中、悬停、流式回复或待确认时保持展开。
- 深蓝玻璃材质搭配粉紫、冰蓝与星光金色的爱弥斯视觉主题。

### 设置中心

提供即时预览的本地设置中心，可调整：

- 桌宠尺寸；
- 紧凑与展开对话框尺寸；
- 对话框背景不透明度（可至完全不透明）；
- 字体缩放、动画速度、自动收缩时间；
- 窗口置顶与开机启动。

UI 偏好保存在本机，不影响 Hermes 的会话、记忆与任务。

### Hermes 原生集成

- 通过 `hermes_platform/ameath_desktop` 提供 Hermes 桌面平台插件。
- 桌宠连接现有 Gateway；Gateway 不可用时进入等待状态，不创建额外的 Hermes 副本。
- 支持用户消息、状态、流式草稿、最终回复、澄清、批准/拒绝和任务通知等原生事件。
- 爱弥斯人格可作为 Hermes 的人格配置使用。

### 面向新手的安装方案

- 使用 PyInstaller 打包桌宠前端，并由 Inno Setup 生成 Windows 安装程序。
- 支持联网安装包与完整离线安装包的构建流程。
- 首次启动向导支持 DeepSeek、OpenAI、OpenAI 兼容接口和 Ollama。
- API Key 使用当前 Windows 用户加密保存，不在设置页或日志中明文显示。
- 正式安装版使用独立 Hermes 运行时，不读取、修改或停止用户既有的 `D:\hermes`。
- 安装程序可自选安装目录，并提供桌面、开始菜单和可选开机启动入口。

## 目录结构

```text
src/digital_pet/                 桌宠应用、窗口、会话与设置实现
hermes_platform/ameath_desktop/  Hermes 原生桌面平台插件
hermes_skills/                   爱弥斯相关的 Hermes 技能定义
packaging/                       PyInstaller、Inno Setup 与运行时构建脚本
tests/                           自动化测试
README.md                        日常使用与构建说明
```

## 开发运行

```powershell
cd 'E:\digital pet'
.\run.bat
```

安装包构建方式、运行环境要求及首次使用说明见 [README.md](README.md)。

## 素材与发布说明

本项目所用的游戏提取素材仅限个人本地使用。它们位于已忽略的本地目录中，不会被提交或随公开仓库、安装包重新分发。公开版本仅包含桌宠代码、配置样例、测试、平台插件与构建脚本。

## 后续方向

- 在干净的 Windows 10/11 环境中完成安装包全流程验证；
- 完善公开仓库的截图、许可证与贡献说明；
- 加入持续集成、版本发布与安装包签名；
- 持续丰富爱弥斯的状态动画与 Hermes 管家能力。
