# 爱弥斯桌宠使用说明与测试清单

适用安装包：`Ameath-1.0.0-offline-setup.exe`。

本清单按正常使用顺序排列。每完成一项可在前方打勾；出现异常时请记录步骤、截图与大致时间，再通过“导出诊断包”保存信息。

## 0. 安装与首次启动

### 0.1 覆盖安装

- [ ] 退出正在运行的爱弥斯（托盘右键菜单选择“退出爱弥斯”）。
- [ ] 运行新版安装包并保持原安装目录。
- [ ] 安装完成后启动爱弥斯。

预期：旧程序文件会被替换；`%LOCALAPPDATA%\Ameath` 的桌宠外观、后端选择和独立模式凭据仍保留；`D:\hermes` 不会被删除或覆盖。

### 0.2 单实例与托盘

- [ ] 连续双击两次爱弥斯快捷方式。
- [ ] 在设置窗口打开时再次双击爱弥斯快捷方式。
- [ ] 确认任务管理器中只有一个 `Ameath.exe`。
- [ ] 点击窗口关闭按钮。
- [ ] 从系统托盘图标右键菜单选择“显示 / 隐藏”。
- [ ] 托盘中依次测试“打开聊天”“设置”“重新连接 Hermes”“退出爱弥斯”。

预期：第二次启动只唤醒原窗口，不会新建桌宠或设置窗口；关闭按钮默认隐藏到托盘；只有“退出爱弥斯”真正退出程序。

## 1. Hermes 后端选择

### 1.1 使用本机 Hermes

前提：本机存在可用 Hermes，例如 `D:\hermes`，其中包含 `config.yaml`、Hermes Python 与 CLI 启动器。

- [ ] 打开“设置”→“本机 Hermes”。
- [ ] 点击“切换或重新检测 Hermes”。
- [ ] 确认提示框可点击、不会被设置窗口挡住。
- [ ] 按提示重启爱弥斯。
- [ ] 在首次选择框中点击“使用本机 Hermes”。

预期：爱弥斯仅接入本机 Hermes；不会显示或复制 API Key；模型、人格与工具设置显示为“由原 Hermes 管理”。

### 1.2 独立爱弥斯模式

- [ ] 再次进入“切换或重新检测 Hermes”。
- [ ] 重启爱弥斯，在选择框中点击“为爱弥斯单独配置”。
- [ ] 完成模型服务引导并发送一条消息。

预期：爱弥斯使用 `%LOCALAPPDATA%\Ameath\hermes`；不会修改 `D:\hermes`。

- [ ] 在独立模式打开“设置”→“Hermes”。
- [ ] 点击“切换或重新检测 Hermes”，确认退出提示。
- [ ] 重启后选择“使用本机 Hermes”。

预期：独立模式同样可以切换回本机 Hermes，不需要重置爱弥斯数据。

### 1.3 暂不设置与探测失败

- [ ] 在首次选择框中点击“暂不设置”。
- [ ] 重启爱弥斯。
- [ ] 将测试用 Hermes 的 `config.yaml` 临时改名后再启动爱弥斯（只在可恢复的测试副本上操作）。

预期：暂不设置不会保存选择，本次启动退出，下次仍会询问；配置缺失、YAML 损坏、缺少 Python 或插件不兼容时，会显示中文原因与建议，不会错误要求重复输入 API Key。

## 2. Gateway、卡顿与安全性

### 2.1 正常连接

- [ ] 在共享 Hermes 模式下先启动原 Hermes Gateway，再启动爱弥斯。
- [ ] 观察聊天状态从“正在验证 Hermes Gateway…”变为“已连接 Hermes Gateway”。
- [ ] 等待至少 1 分钟，拖动桌宠、展开聊天并输入文字。

预期：验证期间界面可操作；不会每隔几秒闪出 CMD 或 PowerShell 黑框；连接后不会反复验证同一 Gateway。

### 2.2 断开与重连

- [ ] 正常运行时关闭原 Hermes Gateway。
- [ ] 确认爱弥斯显示“等待本机 Hermes Gateway”或断开状态。
- [ ] 重新启动原 Hermes Gateway。
- [ ] 在托盘菜单中点击“重新连接 Hermes”。

预期：共享模式只等待和连接，不会自动终止或重启原 Hermes。Gateway 恢复后，爱弥斯自动或手动重新连接。

### 2.3 身份校验保护

- [ ] 在共享模式下点击设置中的“应用 Hermes 并重启”以外的切换功能，确认不会终止原 Hermes。
- [ ] 如要测试重启，仅在没有重要任务时通过原 Hermes 自己的方式重启。

预期：爱弥斯只会在运行时描述文件 PID、进程归属和 Hermes 路径都通过最新校验时终止自己的独立 Gateway；共享 Hermes 或身份不明的进程绝不自动终止。

## 3. 聊天与 Markdown

### 3.1 基础聊天

- [ ] 点击紧凑卡片或右上角展开图标。
- [ ] 输入一条消息，按 Enter 发送。
- [ ] 输入多行消息，按 Shift+Enter 换行，再点击发送图标。
- [ ] 收到回复后收起对话。

预期：Enter 发送、Shift+Enter 换行；展开、收起、发送均使用图标而非大文字；图标悬停会显示中文说明。

### 3.2 Markdown 显示

向爱弥斯发送：

```text
请用 Markdown 回复：一级标题、粗体、斜体、项目列表、引用、https 链接、行内代码和一段 Python 代码块。
```

- [ ] 验证标题、粗斜体、列表、引用和代码格式正常。
- [ ] 点击 `https` 链接。
- [ ] 尝试让回复包含 `javascript:` 或 HTML 标签。

预期：仅助手消息按安全 Markdown 渲染；链接只允许 `http`、`https`、`mailto` 并仅在点击后打开；HTML 与不安全协议不会执行；代码块为深色等宽字体且可横向滚动。

## 4. 界面、图标与可访问性

- [ ] 检查紧凑卡片右侧的展开图标。
- [ ] 展开后检查设置、收起和发送图标。
- [ ] 使用 Tab 在输入框、发送、设置、收起等控件间移动焦点；使用空格或 Enter 激活。
- [ ] 在 Windows 缩放 100%、125%、150% 和 200% 下重复查看。
- [ ] 在“设置”中调整对话框透明度、宽度、高度与字体缩放。

预期：常用操作图标没有方框或乱码；鼠标悬停显示中文 tooltip；键盘可操作；批准/拒绝等高风险动作仍保留中文；聊天正文保持可读且不会因按钮图标化而变小。

## 5. 桌宠行为与游戏模式

- [ ] 拖动桌宠到主屏和副屏不同位置，退出后重新启动。
- [ ] 打开一个全屏程序或游戏。
- [ ] 在托盘菜单依次切换“进入游戏模式”和“退出游戏模式”。
- [ ] 进入和退出游戏模式后确认桌宠仍可从托盘“显示 / 隐藏”找回。
- [ ] 在设置中关闭“主动互动”，等待一段时间。
- [ ] 再开启主动互动，并设置安静时段。

预期：窗口位置会恢复并保持可见；全屏时自动降层、暂停主动互动；游戏模式仅当前会话有效，不会改写“始终置顶”；退出游戏模式后恢复原置顶偏好；关闭主动互动后不再主动弹出，但用户仍可聊天。

## 6. 设置与诊断

- [ ] 打开设置，修改桌宠尺寸、动画速度、透明度和开机启动，再点击“保存桌宠设置”。
- [ ] 共享模式下确认无法编辑原 Hermes 的模型、人格和工具。
- [ ] 从托盘选择“导出诊断包”。
- [ ] 打开生成的 zip，确认其不含聊天正文、API Key、token、配置全文、用户名或完整本地路径。

预期：桌宠外观保存后立即生效；共享模式不会改写原 Hermes 配置；诊断包可用于反馈问题且内容已脱敏。

## 7. 发现问题时的反馈模板

请附上以下内容：

```text
爱弥斯版本：
运行模式：共享 Hermes / 独立爱弥斯
Windows 版本与缩放比例：
发生时间：
操作步骤：
实际表现：
预期表现：
截图或录屏：
诊断包路径（如已导出）：
```

不要发送 API Key、token、`config.yaml` 全文或聊天中的敏感内容。
